name: Generate Snake

on:
  schedule:
    - cron: "0 3 * * *"   # tiap hari 03:00 UTC
  workflow_dispatch:

concurrency:
  group: snake-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Generate Snake (Light + Dark)
        uses: Platane/snk@v3
        with:
          github_user_name: Larusdi
          # kustom palet & warna snake/dots
          outputs: |
            dist/snake.svg?palette=github-light&color_snake=%23007BFF&color_dots=%23000000,%23373737,%23A0A0A0,%23FFFFFF
            dist/snake-dark.svg?palette=github-dark&color_snake=%23A3FF12&color_dots=%23FFFFFF,%238A8A8A,%233E4C59,%230B0F19

      - name: Upload preview artifact
        uses: actions/upload-artifact@v4
        with:
          name: snake-preview
          path: dist

      - name: Publish to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
